package com.example.challenge_4_ilyasa_adam_naufal

import android.app.Application
import android.content.Context
import android.view.Menu
import androidx.lifecycle.AndroidViewModel
import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import androidx.lifecycle.ViewModel

class MainViewModel(context: Application) : AndroidViewModel(context) {

	/*private var _menuChart: MutableLiveData<List<ItemMenu>> = MutableLiveData(arrayListOf())
	val menuChart: LiveData<List<ItemMenu>> get() = _menuChart

	var amount: MutableLiveData<Int> = MutableLiveData(0)

	fun addToChart(menu: ItemMenu) {
		val arrayList = arrayListOf<ItemMenu>()
		arrayList.add(menu)
		_menuChart.value = arrayList
	}*/

}